<h1>利用者一覧</h1>
 
<table>
  <thead>
    <tr>
      <th>氏名</th>
      <th>部署</th>
    </tr>
  </thead>
 
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.name, user_path(user) %></td>
        <td><%= user.department %></td>
      </tr>
    <% end %>

    <%= link_to '新規ユーザ登録', new_user_path %>
  </tbody>
</table>